{
  "name": "trk/processwire-console",
  "description": "A professional CLI tool for ProcessWire CMS.",
  "version": "0.2.0",
  "type": "library",
  "license": "MIT",
  "require": {
    "php": ">=8.3",
    "symfony/console": "^7.0",
    "laravel/prompts": "^0.3.13"
  },
  "autoload": {
    "psr-4": {
      "Totoglu\\Console\\": "src/"
    }
  },
  "bin": [
    "bin/wire"
  ],
  "scripts": {
    "post-install-cmd": [
      "php -r \"if (!file_exists('../../wire')) { symlink('vendor/bin/wire', '../../wire'); echo 'Linked wire executable to project root.\\n'; } else { echo 'wire executable already exists in project root.\\n'; }\""
    ],
    "post-update-cmd": [
      "@post-install-cmd"
    ]
  }
}
